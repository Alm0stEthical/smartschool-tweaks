{
  "name": "smartschool-tweaks",
  "scripts": {
    "build": "bun build ./src/background.ts ./src/content.ts ./src/index.ts --outdir ./dist --target browser --minify && powershell -Command \"Copy-Item src/styles.css dist/styles.css\" && powershell -Command \"Compress-Archive -Path manifest.json, index.html, dist, icons -DestinationPath smartschool-tweaks.zip -Force\"",
    "dev": "bun build --watch ./src/background.ts ./src/content.ts ./src/index.ts --outdir ./dist --target browser",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/chrome": "^0.1.32",
    "prettier": "^3.7.4",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@types/node": "^25.0.3"
  }
}
